# 사용량 세기 — 켜는 법

지금은 **꺼져 있습니다.** 아래 두 줄을 채우면 켜집니다. 채우기 전까지는
앱이 밖으로 아무것도 보내지 않습니다.

## 1. Firebase 준비 (5분)

1. <https://console.firebase.google.com> → 프로젝트 만들기 (기존 프로젝트를 써도 됩니다)
2. **Realtime Database** → 데이터베이스 만들기 → 위치는 **asia-southeast1**
3. **잠금 모드**로 시작한 뒤, 규칙을 이렇게 바꿉니다

```json
{
  "rules": {
    "stat": {
      ".read": false,
      ".write": "!data.exists() || newData.exists()",
      "$day": { "$batch": { ".validate": "newData.hasChildren(['a','t','v'])" } }
    }
  }
}
```

- `.read: false` — **주소를 알아도 남이 읽을 수 없습니다**
- 쓰기만 열어 둡니다. 익명 카운터라 누가 장난쳐도 숫자만 늘 뿐, 나가는 정보는 없습니다
- Apps Script 는 서비스 계정으로 읽습니다 (아래 3번)

4. 데이터베이스 주소를 복사합니다 — `https://<이름>-default-rtdb.asia-southeast1.firebasedatabase.app`

## 2. 앱 켜기 (한 줄)

`js/stat.js` 를 열어 맨 위 한 줄만 채웁니다.

```js
var ENDPOINT = 'https://여기에-복사한-주소';
```

그리고 평소처럼 올리면 끝입니다. 홈 화면 아래 고지에
*"어느 Error 를 많이 찾는지 익명으로만 셉니다"* 한 줄이 자동으로 나타납니다.

## 3. 시트로 옮기기

1. 새 스프레드시트 → 확장 프로그램 → **Apps Script**
2. `실마리_사용량_AppsScript.gs` 내용을 붙여넣고 `FIREBASE_URL` 을 같은 주소로 채웁니다
3. 서비스 계정 읽기 권한이 필요합니다 — 간단히 하려면 규칙의 `.read` 를
   `false` 대신 아래처럼 두고, 주소를 아는 사람만 보게 하십시오

```json
".read": true
```

  이 안에는 **기기 번호 · 날짜 · 장비 · Error 이름**만 있습니다. 거래처도
  사람 이름도 없습니다. 그래도 완전히 막고 싶으면 서비스 계정을 쓰십시오.

4. `setup()` 을 한 번 실행 → 매일 새벽 3시에 저절로 돕니다
5. 처음에는 `pull()` 을 손으로 눌러 값이 들어오는지 봅니다

## 무엇이 보이나

| 시트 | 내용 |
|---|---|
| `raw` | 옮겨온 기록 그대로 |
| `일자별` | 날짜마다 **몇 명이 몇 번** 열었나 |
| `Error별` | 어느 Error 를 **몇 번 · 몇 명이** 봤나 (많이 본 순) |

`Error별` 이 제일 값집니다. 지금은 **CRM 발생 건수**로 순서를 정하고 있는데,
실제로 찾아보는 것과 다를 수 있습니다. 다르면 그쪽을 먼저 보강하면 됩니다.

## 팀에 알릴 것

몰래 세면 도구를 안 믿게 됩니다. 이렇게 말할 수 있습니다.

> 어느 Error 를 많이 찾는지만 익명으로 셉니다. **누가 봤는지는 기록하지 않습니다.**
> 검색어도, 작업 기록도 보내지 않습니다. 다음에 무엇을 보강할지 정하는 데만 씁니다.

이건 사실 그대로입니다 — `js/stat.js` 를 열면 보내는 것이 전부 적혀 있습니다.

## 끄고 싶으면

`js/stat.js` 의 `ENDPOINT` 를 다시 빈 문자열로 두고 올리면 됩니다.
코드를 지울 필요 없이 통째로 꺼집니다.
