# 배포 절차 — GitHub Pages

처음 한 번만 하면 됩니다. **스크립트로 하면 5분**, 손으로 하면 15분입니다.
이후 갱신은 스크립트 한 번이면 끝납니다.

---

## 0. 올리기 전에 확인된 것

이 폴더는 그대로 인터넷에 올라갑니다. 아래는 이미 확인했습니다.

| 항목 | 상태 |
|---|---|
| 거래처(기관) 실명 | **0건** — 전부 `대학병원 A` 형태로 가림 (작업 내용 본문 19곳 포함) |
| CRM 원본 엑셀 · 부품 이력 | 없음 (이 폴더에 애초에 들어가지 않음) |
| Service Manual PDF | 없음 (본문에서 뽑은 절차 텍스트만) |
| 검색엔진 노출 | `robots.txt` + `noindex` 로 차단 |
| 데이터 잠금 | **AES-256-GCM** — 비밀번호 없이는 파일을 받아도 읽을 수 없음 |
| 서브 경로(`/저장소이름/`) 동작 | 실제 서버로 확인 — 설치·오프라인·검색 전부 정상 |

**데이터는 비밀번호로 잠겨 있습니다.** 저장소가 공개여도 파일을 받아 열어 보면
알아볼 수 없는 바이너리입니다. 비밀번호를 아는 사람만 볼 수 있습니다.

**남는 위험**

- 비밀번호 하나를 다 같이 씁니다. **새어 나가면 바꾸기 전까지 막을 수 없습니다.**
- 누가 언제 봤는지 **기록이 남지 않습니다.**
- 파일을 받아 두고 비밀번호를 계속 시도해 볼 수 있습니다(오프라인 대입).
  한 번 시도에 시간이 걸리게 해 두었지만, **짧은 비밀번호는 그래도 뚫립니다.**

개인별 계정과 접속 기록이 필요하면 `대안` 절을 보십시오.

---

## 1. 비밀번호 정하고 데이터 잠그기

```bash
python build_mobile.py      # 데이터 만들기
python encrypt_data.py      # 비밀번호를 두 번 물어봅니다
python make_deploy.py       # 배포 킷 (잠금 여부를 확인해 알려 줍니다)
```

**비밀번호 규칙 — 이게 유일한 자물쇠입니다.**

- 단어 **네 개 이상**을 이어 붙입니다. 예: `sysmex-xn-piercer-2026`
- 장비명·회사명만으로는 안 됩니다. 남이 떠올릴 수 있는 조합은 피하십시오.
- 8자 미만은 스크립트가 거부합니다.

비밀번호는 **어디에도 저장되지 않습니다.** `_auth.json` 에는 salt 와 확인용 조각만
들어 있어 거기서 비밀번호를 되찾을 수 없습니다. 잊으면 다시 잠그는 수밖에 없습니다.

> 비밀번호를 바꾸려면 `build_mobile.py` → `encrypt_data.py` 를 다시 돌리고
> `data/` 와 `sw.js` 를 올리면 됩니다. 기존 사용자는 다음 접속 때 다시 묻습니다.

## 2. 올리기 — 방법 A: 스크립트 (권장)

저장소 만들기 · 파일 올리기 · Pages 켜기를 **한 번에** 합니다. git 을 깔지 않아도 됩니다.

### 2-A-1. 저장소 먼저 만들기 (한 번만, 30초)

1. https://github.com/new
2. **Repository name**: `sysmex-ts` (원하는 이름으로. 이 이름이 URL 에 들어갑니다)
3. **Public** 선택
4. **`Add a README file` 을 체크합니다** ← 중요합니다.
   비어 있는 저장소에는 파일을 올릴 기준점이 없어 스크립트가 멈춥니다.
5. **Create repository**

> 저장소 만들기는 웹에서 합니다. 토큰에 저장소 생성 권한까지 주면
> 그 토큰이 계정 전체에 손댈 수 있게 되기 때문입니다.
> 아래 토큰은 **이 저장소 하나에만** 통하도록 만듭니다.

### 2-A-2. 토큰 발급 (한 번만)

1. https://github.com/settings/tokens?type=beta → 초록색 **Generate new token**
2. **Token name**: `sysmex-ts-deploy` · **Expiration**: 90 days 정도
3. **Repository access**: `Only select repositories` → 방금 만든 **`sysmex-ts`** 를 고릅니다
4. **Permissions → Repository permissions** 에서 두 가지만 **Read and write** 로 바꿉니다:

   | 항목 | 왜 필요한가 |
   |---|---|
   | Contents | 파일을 올리기 위해 |
   | Pages | Pages 를 켜기 위해 |

   (Metadata 는 저절로 Read-only 로 켜집니다. 그대로 두십시오)

5. 맨 아래 **Generate token** → 화면에 나온 `github_pat_...` 를 복사합니다.
   **이 화면을 벗어나면 다시 볼 수 없습니다.**

> 토큰은 비밀번호와 같습니다. 메신저·메일로 보내지 마십시오.
> 스크립트는 입력받아 쓰기만 하고 어디에도 저장하지 않습니다.
> 다 쓰고 나면 같은 화면에서 지워도 됩니다 (갱신할 때 다시 만들면 됩니다).

### 2-A-3. 실행

배포 킷 zip 을 푼 폴더에서:

```powershell
.\github_deploy.ps1
```
```bash
./github_deploy.sh            # macOS · Git Bash
```

저장소 이름과 토큰을 물어봅니다. 그 다음은 알아서 진행하고,
끝나면 **주소를 알려 주고 실제로 열리는지까지 확인**합니다.
(저장소를 못 찾으면 만드는 방법을 알려 주고 멈춥니다)

> PowerShell 이 스크립트 실행을 막으면(빨간 글씨) 이 창에서만 한 번:
> `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass` 를 치고 다시 실행하십시오.

**여기까지 되면 3번으로 건너뛰십시오.**

---

## 2. 올리기 — 방법 B: 손으로

스크립트가 막히거나, 직접 눈으로 확인하며 하고 싶을 때.

### 2-B-1. 저장소 만들기

1. https://github.com/new
2. **Repository name**: `sysmex-ts` (원하는 이름으로. 이 이름이 URL 에 들어갑니다)
3. **Public** 선택
4. 나머지는 건드리지 말고 **Create repository**

> `Add a README file` 은 체크하지 마세요. 비어 있는 편이 다음 단계가 쉽습니다.

### 2-B-2. 파일 올리기

방금 만든 저장소 화면에서 **uploading an existing file** 링크를 누릅니다.

**배포 킷 폴더 안의 것들을 통째로 끌어다 놓습니다.**
폴더 자체를 올리면 안 됩니다 — 그러면 주소 뒤에 폴더 이름이 하나 더 붙습니다.

올라가야 하는 것:

```
index.html   sw.js   manifest.webmanifest   robots.txt   .nojekyll
css/   js/   icons/   data/   README.md
```

(`배포절차.md` · `설치안내.md` · `github_deploy.*` 는 올릴 필요 없습니다)

맨 아래 **Commit changes** 를 누릅니다. 파일이 30개 정도라 1~2분 걸립니다.

> **`.nojekyll` 이 안 올라갔는지 확인하세요.** 점으로 시작해 탐색기에서 숨겨져 있을 수 있습니다.
> 없어도 대부분 동작하지만, 있으면 GitHub 가 파일을 임의로 걸러내는 일이 없습니다.
> 안 보이면: 저장소 화면 → **Add file** → **Create new file** → 이름에 `.nojekyll` 만 치고 저장.

### 2-B-3. Pages 켜기

1. 저장소 상단 **Settings**
2. 왼쪽 메뉴 **Pages**
3. **Source**: `Deploy from a branch`
4. **Branch**: `main` / `/ (root)` → **Save**
5. 1~2분 뒤 새로고침하면 위쪽에 주소가 뜹니다:

```
https://<GitHub아이디>.github.io/sysmex-ts/
```

## 3. 확인

휴대폰으로 그 주소를 열어 봅니다.

- [ ] **비밀번호 화면이 먼저 뜬다**
- [ ] 비밀번호를 넣으면 장비 목록이 나온다
- [ ] 브라우저 주소창에 `.../data/devices.json.enc` 를 직접 쳐 보면 **알아볼 수 없는 파일**이 받아진다
- [ ] 검색창에 `141010` → `RBC/HGB chamber not draining` 이 나온다
- [ ] 맨 아래 **"오프라인용으로 전부 저장"** 버튼이 있다
- [ ] 저장 후 **비행기 모드**로 다시 열어도 검색까지 된다

여기까지 되면 운영 준비가 끝난 것입니다.

## 4. FSG 에 안내

`설치안내.md` 의 **주소와 비밀번호 자리를 채워서** 돌리면 됩니다.
그대로 복사해 사내 메신저에 붙여도 되는 분량으로 써 두었습니다.

> 주소와 비밀번호를 **같은 메시지에 함께 보내지 않는 편**이 낫습니다.
> 메신저 기록이 통째로 새어 나가는 경우를 줄일 수 있습니다.

---

## 데이터 갱신 (월 1회)

CRM 을 새로 뽑았을 때:

```bash
./run_all.sh        # Windows: .\run_all.ps1
```

```bash
python encrypt_data.py      # 새로 만든 데이터를 다시 잠근다 (매번 필요)
python make_deploy.py
```

**`encrypt_data.py` 를 빠뜨리면 평문 데이터가 그대로 올라갑니다.**
`make_deploy.py` 가 그 상태를 알려 주니 출력을 꼭 확인하십시오
(`잠금 꺼짐` 이라고 나오면 멈추고 다시 잠그십시오).

그 다음 **`deploy/` 안의 `data/` 폴더와 `sw.js` 두 개만** 저장소에 다시 올립니다.

- **스크립트로 했다면** `github_deploy.ps1` 을 다시 돌리기만 하면 됩니다.
  같은 저장소 이름을 넣으면 알아서 덮어씁니다.
- **손으로 한다면** 저장소의 `data` 폴더에서 **Add file → Upload files** 로 덮어씁니다.
  `sw.js` 도 같은 방법으로 덮어씁니다. **이 파일을 빼먹으면 갱신이 안 잡힙니다** —
  버전 문자열이 여기에 들어 있습니다.

이미 설치한 사람 화면에는 **"새 데이터가 있습니다 → 받기"** 배너가 뜹니다.
누르지 않아도 예전 데이터로 계속 쓸 수 있습니다.

> 갱신 전에 `python tests/test_mobile_data.py` 를 돌려 **전부 통과**를 확인하십시오.
> 원본이 바뀌었는지, 익명화가 빠지지 않았는지 여기서 잡힙니다.

---

## 대안

| 방법 | 되는 것 | 안 되는 것 |
|---|---|---|
| **GitHub Pages 공개 + 데이터 잠금** (지금 절차) | 무료, HTTPS, PWA 설치·오프라인 전부. 비밀번호 없이는 못 읽음 | 공용 비밀번호 하나. 접속 기록 없음 |
| Cloudflare Pages + Access | **개인별 이메일 로그인 · 접속 기록 · 퇴사자 즉시 차단.** 50명까지 무료, 초과 시 1인 월 $7 | 배포처를 옮겨야 함. 설정이 한 단계 더 많음 |
| GitHub Pages 비공개 | 조직 구성원만 | **GitHub Enterprise Cloud 가 필요합니다** (Team 으로는 안 됩니다) |
| 사내 서버 / SharePoint | 보안상 가장 안전 | **HTTPS 가 아니면 오프라인 저장·홈 화면 설치가 안 됩니다** |
| 단일 HTML 파일 배포 | 서버 불필요 | **iPhone 에서 못 엽니다** (iOS 가 로컬 HTML 실행을 막음) |

지금 방식으로 시작했다가 나중에 Cloudflare Access 를 얹는 것은 어렵지 않습니다.
같은 파일을 옮겨 올리면 되고, 데이터 잠금은 그대로 둔 채 접속 인증만 추가됩니다.
다만 **주소가 바뀌면 엔지니어들이 다시 설치해야 합니다.**
